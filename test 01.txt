Hello World
// create new git repository// 1 time only
git init
git remote add origin
git branch -M main


//update git with changes 每次push 都要照顺序打一遍
1）git add .
2）git commit -m "message" //git hub 里面的proj名
3）git push origin main

1）git pull origin main

1)git clone